/* eslint-disable no-unused-vars */
/* eslint-disable indent */

// TODO update this import when renamed
import { action } from 'packages/scandi-dark-theme/src/store/DarkMode/DarkMode.action';

/** @namespace ScandiDarkTheme/Src/Store/DarkMode/Dispatcher/DarkModeDispatcher */
export class DarkModeDispatcher {
  update(payload, dispatch) {
    // TODO implement
    // dispatch(action(payload));
  }
}

export default new DarkModeDispatcher();
